@model IEnumerable<API_Usage.Models.Transaction>
<h2>Transactions</h2>
<hr />

@foreach (var item1 in Model)
{
    @foreach(var item2 in item1.Property1)
  {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item2.effectiveDate)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item2.fullName)
    </td>
</tr>

}

}
